
import React from 'react';
import { Button, Intent } from "@blueprintjs/core";

export default () => {
    return (
        <div>
            <Button
                text="text"
                intent={Intent.SUCCESS}
            />
        </div>
    );
};